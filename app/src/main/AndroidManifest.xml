<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.cercis">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".CercisApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_cercis_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_cercis_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cercis.DayNight"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".StartupActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_startup"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="cercis.cn"
                    android:pathPrefix="/client/open"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <service android:name=".service.NotificationService" />
    </application>

</manifest>